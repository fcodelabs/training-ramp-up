import { UpdateStudentValidationPipe } from './update-student-validation.pipe';

describe('UpdateStudentValidationPipe', () => {
  it('should be defined', () => {
    expect(new UpdateStudentValidationPipe()).toBeDefined();
  });
});
