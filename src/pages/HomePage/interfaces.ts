export interface Person{
    ID : number;
    Name ? : string;
    Gender ? : string;
    Address ? : string;
    MobileNo ?: string;
    DateOfBirth ? : Date;
    Age ?: number;
    inEdit?: boolean;
}